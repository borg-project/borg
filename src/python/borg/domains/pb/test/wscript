def build(context):
    context(
        source = [
            "__init__.py",
            "test_opb.py",
            "test_solvers.py",
            ],
        install_path = "${PYTHONDIR}/borg/domains/pb/test",
        )

