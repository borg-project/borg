#!/bin/bash

ACRIDID_PATH_FILE=~/.quest/paths/acridid

[ -f $ACRIDID_PATH_FILE ] || (echo "Configuration file not present." && exit -1)

R=$(eval "echo $(cat $ACRIDID_PATH_FILE)")

$R/bin/acridid-env python $@

